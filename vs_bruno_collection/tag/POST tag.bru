meta {
  name: POST tag
  type: http
  seq: 1
}

post {
  url: {{baseURL}}/api/tags
  body: json
  auth: inherit
}

body:json {
  {
    "name": "{{$randomCatchPhraseAdjective}} {{$randomCatchPhraseNoun}}"
  }
}

vars:post-response {
  name: req.body.name
  id: res.body
}

assert {
  res.status: eq 201
}

settings {
  encodeUrl: true
  timeout: 0
}
