meta {
  name: GET tag by id
  type: http
  seq: 4
}

get {
  url: {{baseURL}}/api/tags/{{id}}
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
  res.body.name: eq "{{name}}"
}

settings {
  encodeUrl: true
  timeout: 0
}
