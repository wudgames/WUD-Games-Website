meta {
  name: DELETE tag by id
  type: http
  seq: 4
}

delete {
  url: {{baseURL}}/api/tags/{{id}}
  body: none
  auth: inherit
}

assert {
  res.status: eq 204
}

settings {
  encodeUrl: true
  timeout: 0
}
