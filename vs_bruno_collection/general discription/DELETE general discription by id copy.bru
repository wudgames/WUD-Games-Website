meta {
  name: DELETE general discription by id copy
  type: http
  seq: 6
}

delete {
  url: {{baseURL}}/api/generalDiss/{{id}}
  body: none
  auth: inherit
}

assert {
  res.status: eq 204
}

settings {
  encodeUrl: true
  timeout: 0
}
