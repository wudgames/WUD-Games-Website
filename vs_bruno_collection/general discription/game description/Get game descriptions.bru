meta {
  name: Get game descriptions
  type: http
  seq: 4
}

get {
  url: {{baseURL}}/api/gameDiss
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
}

settings {
  encodeUrl: true
  timeout: 0
}
