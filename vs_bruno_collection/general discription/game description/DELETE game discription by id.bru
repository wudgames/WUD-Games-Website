meta {
  name: DELETE game discription by id
  type: http
  seq: 7
}

delete {
  url: {{baseURL}}/api/gameDiss/{{id}}
  body: none
  auth: inherit
}

assert {
  res.status: eq 204
}

settings {
  encodeUrl: true
  timeout: 0
}
