meta {
  name: DELETE general discription by id
  type: http
  seq: 6
}

delete {
  url: {{baseURL}}/api/generalDiss/{{genDis_id}}
  body: none
  auth: inherit
}

assert {
  res.status: eq 409
}

settings {
  encodeUrl: true
  timeout: 0
}
