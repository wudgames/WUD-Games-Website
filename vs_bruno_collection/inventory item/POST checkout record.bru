meta {
  name: POST checkout record
  type: http
  seq: 9
}

post {
  url: {{baseURL}}/api/checkoutRecords
  body: json
  auth: inherit
}

body:json {
  {
    "checkoutTime": "{{$isoTimestamp}}",
    "returnedTime": null,
    "peoplePlaying": {{$randomInt}},
    "resipeantName": "{{$randomFullName}}",
    "inventoryItems": [{{id}}]
  }
}

vars:post-response {
  currentCheckout_id: res.body
}

assert {
  res.status: eq 201
}

settings {
  encodeUrl: true
  timeout: 0
}
