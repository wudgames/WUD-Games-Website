meta {
  name: DELETE inventory item by id
  type: http
  seq: 13
}

delete {
  url: {{baseURL}}/api/inventoryItems/{{id}}
  body: none
  auth: inherit
}

assert {
  res.status: eq 204
}

settings {
  encodeUrl: true
  timeout: 0
}
