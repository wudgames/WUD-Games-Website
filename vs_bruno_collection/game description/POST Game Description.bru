meta {
  name: POST Game Description
  type: http
  seq: 1
}

post {
  url: {{baseURL}}/api/gameDiss
  body: json
  auth: inherit
}

body:json {
  {
    "minPlayers": {{$randomInt}},
    "maxPlayers": {{$randomInt}}
  }
}

vars:post-response {
  responseID: res.body
  minPlayers: req.body.minPlayers
  maxPlayers: req.body.maxPlayers
}

assert {
  res.status: eq 201
}

settings {
  encodeUrl: true
  timeout: 0
}
