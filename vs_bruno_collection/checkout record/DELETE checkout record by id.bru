meta {
  name: DELETE checkout record by id
  type: http
  seq: 11
}

delete {
  url: {{baseURL}}/api/checkoutRecords/{{id}}
  body: none
  auth: inherit
}

assert {
  res.status: eq 204
}

settings {
  encodeUrl: true
  timeout: 0
}
