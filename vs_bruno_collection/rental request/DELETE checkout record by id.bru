meta {
  name: DELETE checkout record by id
  type: http
  seq: 5
}

delete {
  url: {{baseURL}}/api/checkoutRecords/{{checkoutrecord_id}}
  body: none
  auth: inherit
}

assert {
  res.status: eq 409
}

settings {
  encodeUrl: true
  timeout: 0
}
