meta {
  name: GET rental requests
  type: http
  seq: 6
}

get {
  url: {{baseURL}}/api/rentalRequests
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
}

settings {
  encodeUrl: true
  timeout: 0
}
