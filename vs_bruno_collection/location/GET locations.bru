meta {
  name: GET locations
  type: http
  seq: 2
}

get {
  url: {{baseURL}}/api/locations
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
}

settings {
  encodeUrl: true
  timeout: 0
}
