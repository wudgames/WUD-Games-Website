meta {
  name: DELETE location by id
  type: http
  seq: 4
}

delete {
  url: {{baseURL}}/api/locations/{{id}}
  body: none
  auth: inherit
}

assert {
  res.status: eq 204
}

settings {
  encodeUrl: true
  timeout: 0
}
