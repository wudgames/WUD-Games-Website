meta {
  name: GET location by id
  type: http
  seq: 3
}

get {
  url: {{baseURL}}/api/locations/{{id}}
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
  res.body.name: eq "{{name}}"
}

settings {
  encodeUrl: true
  timeout: 0
}
