info:
  name: DELETE general discription by id
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{baseURL}}/api/generalDiss/{{genDis_id}}"
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "409"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
