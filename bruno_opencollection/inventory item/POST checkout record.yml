info:
  name: POST checkout record
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseURL}}/api/checkoutRecords"
  body:
    type: json
    data: |-
      {
        "checkoutTime": "{{$isoTimestamp}}",
        "returnedTime": null,
        "peoplePlaying": {{$randomInt}},
        "resipeantName": "{{$randomFullName}}",
        "inventoryItems": [{{id}}]
      }
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "201"
  actions:
    - type: set-variable
      phase: after-response
      selector:
        expression: res.body
        method: jsonq
      variable:
        name: currentCheckout_id
        scope: runtime

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
