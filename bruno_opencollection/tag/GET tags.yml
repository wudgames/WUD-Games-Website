info:
  name: GET tags
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseURL}}/api/tags"
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
