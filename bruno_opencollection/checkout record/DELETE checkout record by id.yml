info:
  name: DELETE checkout record by id
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{baseURL}}/api/checkoutRecords/{{id}}"
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "204"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
