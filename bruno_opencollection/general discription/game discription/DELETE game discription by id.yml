info:
  name: DELETE game discription by id
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseURL}}/api/gameDiss/{{id}}"
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "204"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
