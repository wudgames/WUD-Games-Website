info:
  name: Get game expansion descriptions
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseURL}}/api/gameExpansionDiss"
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
